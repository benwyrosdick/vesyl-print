# Optional but required for Phase D ActionCable push (PrintNodeChannel).
# Pull-path jobs work without it. Debian: apt install python3-websocket
websocket-client>=1.6.0

# Required for OTA Ed25519 signature verification.
# Debian: apt install python3-cryptography
cryptography>=41.0.0
